@model IEnumerable<Product>

@{
    ViewBag.Title = "Məhsullar";
    Layout = "_Layout";
}


    <!-- Content Header (Page header) -->
    <section class="content-header">
      <h1>
        Məhsullar
      </h1>
      <ol class="breadcrumb">
        <li><a asp-controller="Home" asp-action="Index"><i class="fa fa-dashboard"></i> Əsas</a></li>
        <li class="active">Məhsullar</li>
      </ol>
    </section>

    <!-- Main content -->
    <section class="content">

      <!-- Default box -->
      <div class="box">
            <!-- /.box-header -->
            <div class="box-body">
              <partial name="_SuccessMessagePartial"/>
              <partial name="_DangerMessagePartial"/>
              <partial name="_WarningMessagePartial"/>
              <div class="form-group">
                <a asp-controller="Product" asp-action="Create" class="btn btn-success">Yeni məhsul əlavə et</a>
              </div>
              <table id="example1" class="table table-bordered table-striped">
                <thead>
                <tr>
                  <th>Ad</th>
                  <th>Mətn</th>
                  <th>Şəkil</th>
                  <th>Hərəkətlər</th>
                  <th style="display: none"></th>
                  <th style="display: none"></th>
                </tr>
                </thead>
                <tbody>
                @foreach (var product in Model)
                {
                  <tr>
                    <td>@product.Name</td>
                    <td >@Html.Raw(product.Description)</td>
                    <td>
                      <img src="~/images/shop/@product.Image" alt="@product.Image" width="100">
                    </td>
                    <td>
                      <a asp-action="Edit" asp-controller="Product" asp-route-id="@product.Id" class="fa fa-pencil"></a>
                      <a asp-action="Delete" asp-controller="Product" asp-route-id="@product.Id" class="fa fa-remove"></a>
                    </td>
                    <td style="display: none"></td>
                    <td style="display: none"></td>
                  </tr>
                }
                </tbody>
              </table>
            </div>
            <!-- /.box-body -->
          </div>
      <!-- /.box -->

    </section>
<!-- /.content -->
